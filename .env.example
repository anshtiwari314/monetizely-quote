# Local (default): data/monetizely.db under project root
# DATABASE_PATH=./data/monetizely.db

# Vercel (ephemeral — resets on cold start unless you use persistent storage)
# DATABASE_PATH=/tmp/monetizely.db
